version: 2.1

orbs:
  docker: circleci/docker@1.5.0
  maven: circleci/maven@1.2.0
  secrethub: secrethub/cli@1.1.0

executors:
  java15:
    docker:
      - image: 'cimg/openjdk:15.0'
workflows:
  build-master: 
    jobs:
      - maven/test:
          executor: java15
      - docker/publish:
          image: developerexp/technology-api
          tag: latest
          pre-steps:
            - secrethub/env-export:
                secret-path: thoughtworks-developer/technology-api/docker/user
                var-name: DOCKER_LOGIN
            - secrethub/env-export:
                secret-path: thoughtworks-developer/technology-api/docker/password
                var-name: DOCKER_PASSWORD
          requires:
            - "maven/test"